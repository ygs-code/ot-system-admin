import React, { Component } from "react";


type Props = {
    value?: string;
    onChange?: (value: string) => void;
}

export default (props: Props) => {
      
    return <div>
         asdf 
        </div>

};